import React from "react";
import { PopupFrame } from "../UI";

const SelectDateTime = () => {
  return (
    <PopupFrame frameText="Select Date and time <span className='block text-base'> in your local time zone(westAfrica/Lagos )">
      {"test"}
    </PopupFrame>
  );
};

export default SelectDateTime;
